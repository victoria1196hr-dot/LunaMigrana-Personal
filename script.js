const s=slider,n=num;const load=()=>{let r=JSON.parse(localStorage.getItem('luna')||'[]');total.textContent=r.length;if(r.length){let a=(r.reduce((x,y)=>x+y.i,0)/r.length).toFixed(1);avg.textContent=a;streak.textContent=Math.max(0,7-r.length);history.innerHTML=r.slice(0,5).map(e=>`<div class='entry'><b>${e.d}</b><br>${e.i}/10</div>`).join('');ai.textContent=`Tus últimos ${r.length} registros promedian ${a}/10.`;}};s.oninput=()=>{n.textContent=s.value;moon.style.filter=`brightness(${0.6+s.value/10})`;};save.onclick=()=>{let r=JSON.parse(localStorage.getItem('luna')||'[]');r.unshift({i:+s.value,d:new Date().toLocaleDateString('es-HN')});localStorage.setItem('luna',JSON.stringify(r));load();};load();