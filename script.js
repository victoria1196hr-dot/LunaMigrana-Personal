const names=['Aura','Náuseas','Luz','Sonido','Cuello'];const chips=document.getElementById('chips');let sel=[];names.forEach(n=>{let b=document.createElement('div');b.className='chip';b.textContent=n;b.onclick=()=>{b.classList.toggle('on');sel=b.classList.contains('on')?[...sel,n]:sel.filter(x=>x!=n)};chips.appendChild(b)});pain.oninput=()=>v.textContent=pain.value;function render(){let r=JSON.parse(localStorage.getItem('lunaLast')||'null');last.textContent=r?`${r.fecha} · Dolor ${r.dolor}/10 · ${r.med} · ${r.sintomas.join(', ')||'Sin síntomas'}`:'Aún no hay registros.'}save.onclick=()=>{localStorage.setItem('lunaLast',JSON.stringify({fecha:new Date().toLocaleString('es-HN'),dolor:pain.value,med:med.value,sintomas:sel}));render();alert('Crisis guardada 🌙')};render();